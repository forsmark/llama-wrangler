import { create } from 'zustand'

// Global Zustand store — stub for Task 1.
// State slices will be added as features are implemented.

interface AppState {
  // placeholder
}

export const useAppStore = create<AppState>()(() => ({}))
